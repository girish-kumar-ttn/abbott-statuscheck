pipeline {
    agent any

  

    stages {
        stage('Checking URLs') {
            steps {
               
                
                sh "rm -rf down.txt"       
                sh "/var/lib/jenkins/workspace/abbott-statuscheck/statuscheck.sh"
archiveArtifacts artifacts: 'down.txt', excludes: 'output/*.md'
            }
        }
    }
}
